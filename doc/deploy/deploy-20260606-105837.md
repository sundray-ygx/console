# Console Deployment Record — 20260606-105837

## Scope

Redeploy Console frontend after homepage optimization:

- Removed Aliyun Pan and WebDAV from homepage self-hosted service shortcuts.
- Removed homepage Today Overview, OKR Progress, and Knowledge Updates widgets.
- Kept Weekly PDCA, System Status, Self-hosted Services, and Bookmarks.

## Pre-deploy Quality Gate

| Gate | Result | Evidence |
|------|--------|----------|
| Frontend build | PASS | `npm run build` completed with `✓ built in 4.38s` |
| Docker image build | PASS | `docker compose -f /root/.hermes/projects/docker-compose.yml build console` completed |

Formal `/tdd` acceptance/lint/security/design-score artifacts are not present in this prototype repo, so deployment used available project verification: production frontend build plus post-deploy smoke checks.

## Deployment

Command:

```bash
docker compose -f /root/.hermes/projects/docker-compose.yml build console && \
docker compose -f /root/.hermes/projects/docker-compose.yml up -d console
```

Result:

- `projects-console-1` recreated and started.
- `projects-console-api-1` remained running.

## Post-deploy Smoke Tests

| Check | Result | Evidence |
|-------|--------|----------|
| Docker services | PASS | `projects-console-1` Up on `0.0.0.0:3200->80`; `projects-console-api-1` Up on `0.0.0.0:3300->3300` |
| Local frontend | PASS | `curl http://127.0.0.1:3200/` returned HTTP 200, 576 bytes |
| Local API proxy | PASS | `curl http://127.0.0.1:3200/api/system` returned HTTP 200 with `cpu` and `memory` |
| Public frontend | PASS | `curl -L https://console.ygxpro.online/` returned HTTP 200, 576 bytes |
| Service shortcuts | PASS | Public bundle contains `Vaultwarden`; does not contain `Aliyun Pan` or `WebDAV` |
| Dashboard widgets | PASS | Public bundle does not contain `今日概览`, `OKR 进度`, `知识动态`; contains `本周 PDCA`, `系统状态`, `自托管服务`, `常用工具` |

## Rollback

Not triggered. All smoke checks passed.
