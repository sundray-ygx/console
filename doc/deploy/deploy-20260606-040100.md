# Console Deployment Record — 20260606-040100

## Scope

PDCA redesign (Plan A) + homepage cleanup:

1. Removed "本周PDCA" from homepage.
2. Rewrote `/api/notion/pdca/current` to return `{ period, plan, review }` structure.
3. Reworked PdcaView.vue from 4-column P/D/C/A kanban to 2-section "计划/复盘" layout.
4. Month/quarter reviews auto-generated from weekly data via rule-based summary.

## Deployment

```bash
docker compose -f /root/.hermes/projects/docker-compose.yml build console-api console
docker compose -f /root/.hermes/projects/docker-compose.yml up -d console-api console
```

## Smoke Tests

| Check | Result |
|-------|--------|
| Container status | ✅ Both running |
| Local frontend :3200 | ✅ HTTP 200 |
| Public frontend console.ygxpro.online | ✅ HTTP 200 |
| Local API proxy /api/system | ✅ HTTP 200 |
| Homepage PDCA removed | ✅ No "本周PDCA" in bundle |
| PDCA API (period/plan/review) | ✅ period, plan, review keys present |
| PDCA month review generated | ✅ month.generated=true, sourceCount=1 |
| PDCA quarter review generated | ✅ quarter.generated=true, sourceCount=1 |

## Files Changed (frontend)
- `src/views/DashboardView.vue` — removed PDCA section, useNotion import, scoreStars, notionTimer
- `src/views/PdcaView.vue` — rewritten to plan/review layout

## Files Changed (backend)
- `console-api/server.mjs` — rewritten pdca/current endpoint
